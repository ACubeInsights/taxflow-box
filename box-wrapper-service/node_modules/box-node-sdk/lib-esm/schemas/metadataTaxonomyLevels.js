import { serializeMetadataTaxonomyLevel } from './metadataTaxonomyLevel.js';
import { deserializeMetadataTaxonomyLevel } from './metadataTaxonomyLevel.js';
import { BoxSdkError } from '../box/errors.js';
import { sdIsList } from '../serialization/json.js';
import { sdIsMap } from '../serialization/json.js';
export function serializeMetadataTaxonomyLevels(val) {
    return {
        ['entries']: val.entries == void 0
            ? val.entries
            : val.entries.map(function (item) {
                return serializeMetadataTaxonomyLevel(item);
            }),
    };
}
export function deserializeMetadataTaxonomyLevels(val) {
    if (!sdIsMap(val)) {
        throw new BoxSdkError({
            message: 'Expecting a map for "MetadataTaxonomyLevels"',
        });
    }
    if (!(val.entries == void 0) && !sdIsList(val.entries)) {
        throw new BoxSdkError({
            message: 'Expecting array for "entries" of type "MetadataTaxonomyLevels"',
        });
    }
    const entries = val.entries == void 0
        ? void 0
        : sdIsList(val.entries)
            ? val.entries.map(function (itm) {
                return deserializeMetadataTaxonomyLevel(itm);
            })
            : [];
    return { entries: entries };
}
//# sourceMappingURL=metadataTaxonomyLevels.js.map