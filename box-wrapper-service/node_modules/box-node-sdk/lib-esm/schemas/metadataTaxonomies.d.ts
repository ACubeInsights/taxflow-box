import { MetadataTaxonomy } from './metadataTaxonomy.js';
import { SerializedData } from '../serialization/json.js';
export interface MetadataTaxonomies {
    /**
     * The limit that was used for these entries. This will be the same as the
     * `limit` query parameter unless that value exceeded the maximum value
     * allowed. The maximum value varies by API. */
    readonly limit?: number;
    /**
     * The marker for the start of the next page of results. */
    readonly nextMarker?: string | null;
    /**
     * The marker for the start of the previous page of results. */
    readonly prevMarker?: string | null;
    /**
     * A list of metadata taxonomies. */
    readonly entries?: readonly MetadataTaxonomy[];
    readonly rawData?: SerializedData;
}
export declare function serializeMetadataTaxonomies(val: MetadataTaxonomies): SerializedData;
export declare function deserializeMetadataTaxonomies(val: SerializedData): MetadataTaxonomies;
